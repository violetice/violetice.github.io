Fluid.boot={};Fluid.boot.registerEvents=function(){Fluid.events.billboard();Fluid.events.registerNavbarEvent();Fluid.events.registerParallaxEvent();Fluid.events.registerScrollDownArrowEvent();Fluid.events.registerScrollTopArrowEvent();Fluid.events.registerImageLoadedEvent()};Fluid.boot.refresh=function(){Fluid.plugins.fancyBox();Fluid.plugins.codeWidget();Fluid.events.refresh()};document.addEventListener("DOMContentLoaded",function(){Fluid.boot.registerEvents();const e=$(".runtime");const t=moment(e.data("start"));$(".runtime_start",e).text(`本站起始于 ${t.format("YYYY-MM-DD")}`);$(".runtime_runDay",e).text(`至今已有 ${moment().diff(t,"days")} 天`)});